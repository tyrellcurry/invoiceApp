services:
  db:
    image: postgres:17
    restart: always
    shm_size: 128mb
    environment:
      - POSTGRES_USER=${POSTGRES_USER}
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD}
      - POSTGRES_DB=${POSTGRES_DB}
    ports:
      # Bound to localhost only: Postgres never needs to be reachable from
      # outside the Pi, the Go binary connects to it over the loopback
      # interface. This is the one difference from ../compose.yaml, which
      # binds to every interface for convenience during local development.
      - "127.0.0.1:5432:5432"
    volumes:
      - invoiceapp_db_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $$POSTGRES_USER -d $$POSTGRES_DB"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  invoiceapp_db_data:
